import React , {useState} from 'react' ;

const merchantPage = () => {
    const [activeNav,setActiveNav] = useState(false);

    return (
        <div className="flex min-h-screen bg-gray-100" style={{ fontFamily: "'Segoe UI', sans-serif" }}>

            {/* SideBar */}

            {/* Main Content Area */}
            {/* Page */}
            {/* Dashboard | directory Tab */}
            {/* merchant Revenue Insight */}
            {/* M2O community overview */}
        
        </div>

    );
};